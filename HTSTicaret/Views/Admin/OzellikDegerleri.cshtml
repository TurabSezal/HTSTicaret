
@{
    ViewBag.Title = "OzellikDegerleri";
    Layout = "~/Views/Shared/AdminLayout.cshtml";
}

@model IEnumerable<HTSTicaret.Models.OzellikDeger>

<section class="panel">
    <header class="panel-heading">
        Özellik Değerleri
    </header>
    <div class="panel-body">
        <div class="adv-table editable-table ">
            <br />
            <div class="clearfix">
                <div class="btn-group">
                    <a href="OzellikDegerEkle" class="btn btn-success">
                        Yeni Değer <i class="icon-plus"></i>
                    </a>
                </div>
            </div>
            <div class="space15"></div>
            <table class="table table-striped table-hover table-bordered" id="editable-sample">
                <thead>
                    <tr>
                        <th>Özellik Değer Adı</th>
                        <th>Açıklama</th>
                        <th>Özellik Tipi</th>
                        <th>Düzenle</th>
                        <th>Sil</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var deger in Model)
                    {
                        <tr class="">
                            <td>@deger.Adi</td>
                            <td>@deger.Aciklama</td>
                            @if (deger.OzellikTip != null)
                            {
                                <td>@deger.OzellikTip.Adi</td>
                            }

                            <td>
                                <a class="edit"
                                   href="/admin/OzellikDegerGuncelle/@deger.Id">
                                    <i class="icon-pencil"></i>
                                    Düzenle
                                </a>
                            </td>
                            <td>
                                <a class="delete"
                                   href="/admin/OzellikDegerSil/@deger.Id">
                                    <i class="icon-trash"></i>
                                    Sil
                                </a>
                            </td>

                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</section>

