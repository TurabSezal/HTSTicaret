
@{
    ViewBag.Title = "UrunOzellikEkle";
    Layout = "~/Views/Shared/AdminLayout.cshtml";
}

@section head
    {
    <script src="~/Content/Templates/AdminTemplates/flatlab/admin/template_content/js/jquery-1.8.3.min.js"></script>
    <script>
        $(document).ready(function myfunction() {
            $("urun").change(function myfunction() {
                var id = $(this).find("option:selected").attr("title");

                $.ajax({
                    type: "GET",
                    url: "/Admin/UrunOzellikTipWidget",
                    data: { katId: kId },
                    success: function myfunction(veri) {
                        $("#tip").html('');
                        $("#tip").append(veri);
                    },
                    error: function myfunction() {

                    }

                });
            })

            $("#tip .m-bot15").change(function myfunction() {
                var id = $(this).val();
                alert(id);
            });
        });

    </script>
    }

@model IEnumerable<HTSTicaret.WebUI.Models.Urun>

<div class="row">
    <h2>Ürün Ekle</h2>
    <div class="container">
        <div class="col-lg-12">
            <section class="panel">
                <header class="panel-heading">
                    Ürün Özellik Ekle
                </header>
                <div class="panel-body" style="margin-top:20px">
                    <form role="form" action="/Admin/UrunOzellikEkle" method="post" class="form-horizontal tasi-form">

                        <div class="form-group">
                            <label class="col-lg-2 control-label">Ürün :</label>
                            <div class="col-lg-10">
                                <select class="form-control m-bot15" id="urun" name="UrunID">
                                    @foreach (var urun in Model)
                                    {
                                        <option value="@urun.Id"
                                                title="@urun.KategoriID"> @urun.Adi </option>
                                    }
                                </select>
                            </div>
                        </div>
                        <div id="tip">

                        </div>
                        <div id="deger">

                        </div>

                        <div class="form-group">
                            <div class="col-lg-offset-2 col-lg-10">
                                <button class="btn btn-danger" type="submit">Kaydet</button>

                            </div>

                        </div>
                    </form>
                </div>
            </section>
        </div>
    </div>
</div>